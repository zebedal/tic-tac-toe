import React from "react";
import ReactDOM from "react-dom/client";
import Game from "./App";
import Application from "./PhoneBook";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Game />
  </React.StrictMode>
);
